---
title: 5. Decision Tree
---
# 5. 0 Introduction
---
A decision tree is a type of supervised learning algorithm designed to replicate the way humans make decisions, often used for classification and regression tasks. Let's build a decision tree to classify cat images using the following data.

|     | Ear Shape($x_1$) | Face Shape($x_2$) | Whiskers($x_3$) | Target($y$) |
| :-: | :---------------: | :---------------: | :-------------: | :---------: |
|  1  |    Pointy (1)     |     Round (1)     |   Present (1)   |      1      |
|  2  |      Floppy       |     Not round     |     Present     |      1      |
|  3  |      Floppy       |       Round       |     Absent      |      0      |
|  4  |      Pointy       |     Not round     |     Present     |      0      |
|  5  |      Pointy       |       Round       |     Present     |      1      |
|  6  |      Pointy       |       Round       |     Absent      |      1      |
|  7  |      Floppy       |     Not round     |     Absent      |      0      |
|  8  |      Pointy       |       Round       |     Absent      |      1      |
|  9  |      Floppy       |       Round       |     Absent      |      0      |
| 10  |      Floppy       |       Round       |     Absent      |      0      |
We can build various trees as below.

![[ML5-1.png]]

The optimal tree looks as follows. Let's take a look at its architecture through this tree.
# 5. 1 Tree Architecture
---
![[ML5-2.png]]

1. Root Node
	- Starting point of tree
	- Will be split by the most important feature
2. Decision Node
	- Decision nodes, like the root node, further split their data using the best possible feature
	- Defined recursively
3. Leaf Node
	- Tree ends in this node
	- Prediction comes out from here
4. Depth
	- Distance from root to the deepest leaf
	- depth = 2 in above tree
5. Parent Node
	- Node right before split
	- In the following explanation, we will denote it as $S$.

# 5. 2 Split
---
As seen in the tree above, each node has the most optimal feature to split the data. We will measure the purity of each split by evaluating how the data is mixed after split

In the following explanation, we will denote the left split as $L$ and the right split as $R$.
## 5. 2. 1 Entropy ; Impurity
---
If we place "face shape" at the root node, the data will be split into two groups of 7 and 3. Among them, only 4 and 1 data are cats, respectively. For example, the left split has following purity:

$$
p=\cfrac{|\{data\in L|y=1\}|}{|L|}=\cfrac{4}{7}
$$

To represent the impurity each set, we define **entropy** function $H:[0,1]\rightarrow[0,1]$ as follow:

$$
H(p)\coloneqq p\log{p}-(1-p)\log(1-p)
$$

Hence, each split of feature "Face Shape" has two $H(4/7)\approx0.99$,  $H(1/3)\approx0.92$, respectively.
## 5. 2. 2 Information Gain
---
To classify cats, we need to construct the tree in a way that reduces entropy. At this point, we combine the entropy of the two splits into a single value to easily compare which feature can reduce more entropy. To give more weights to splits with large number of datas in determining the impurity, we will first use a **weighted average**
$$
\cfrac{|L|}{|S|}\cdot H(4/7)+\cfrac{|R|}{|S|}\cdot H(1/3) = \frac{7}{10}H(4/7)+\frac{3}{10}H(1/3)
$$
And then, to measure how much the impurity has reduced after the split, information gain is (entropy of parent node) - (weighted average). In the example, since the parent node is the root node, its entropy is $H(5/10)$. Therefore, if we place the second feature at the root node, the information gain is as follow:

$$
H(5/10)-\left\{\frac{7}{10}H(4/7)+\frac{3}{10}H(1/3)\right\}\approx0.03
$$

The first and third features yield information gains of about 0.28 and 0.12, respectively. Thus, using the first feature -ear shape- at the root node leads to the purest split.
## 5. 2. 3 Stopping Splitting
---
At each split, we will recursively apply the same principle, choosing the feature that yields the highest information gain. Now, take a look at when we should stop splitting the tree.

1. When node is 100% single class
2. When the tree exceeds the preset maximum depth
	- To prevent overfitting
3. When improvement in purity score are less than the threshold
4. When number of examples in an node is less than the threshold

# 5. 3 Feature Problem
---
So far, we have constructed the tree using only binary features. Now, let’s take a look at how to build a tree with the following features.

## 5. 3. 1 Multiple Categorial Feature
---
Suppose the feature "Ear shape" can take on the values pointy, floppy, and oval. While split into multiple branches can be a solution, we'll introduce an alternative method : **one-hot encoding**.

Instead of assigning values from 0 to 2, we will separate this feature into 3 feature as below:

1. Is ear shape pointy?
2. Is ear shape floppy?
3. Is ear shape oval?

Then, pointy, floppy, and oval ones can take (1, 0, 0), (0, 1, 0), and (0, 0, 1), respectively. Through this, we can build a tree with same method that we learned.

## 5. 3. 2 Continuous Valued Feature
---
Suppose that we have a feature for the weight of the animal. To split into 2 groups, we will transform the weight feature into a question like: "Does the weight exceed a specific threshold?".

To determine the best threshold for a continuous feature like weight, we follow these steps:

1. Sort the data based on the weight values.
2. Try every possible threshold between adjacent values, and for each threshold:
    - Split the data into two groups: one with weight ≤ threshold, and one with weight > threshold.
    - Compute the information gain from this split.
3. Select the threshold that gives the highest information gain.

Therefore, acontinuous valued feature can be transformed into a binary feature using a threshold, and then used in the tree.
# 5. 4 Regression Tree
---
A regression tree is a type of decision tree that predicts continuous values like a regression model.

In regression trees, instead of using entropy, the algorithm evaluates how well the datas are clustered by calculating the (mathematical) variance of target values of splits. Thus, to reduce the variance, we use the following value:

$$
\sigma_{S}-\left\{\cfrac{|L|}{|S|}\sigma_{L}+\cfrac{|R|}{|S|}\sigma_{R}\right\}
$$

# 5. 5 Tree Ensemble
---
The single tree algorithm always performs greedy splits at each step, which makes it difficult to generalize well. That is, changing even one training sample can affect the entropy of root node.

## 5. 5. 1 Sampling with Replacement
---
In following algorithm, we will build a lot of different trees. Since we continue to build trees using the same greedy policy, we need different training sets to build different trees. **Sampling with replacement** is a way to make various training set with original training set as following steps:

1. Set the size of training set, say $m$.
2. Randomly choose one sample from population
3. Put it back into the population
4. Repeat until the training set contains $m$ elements.

A single sampling may contain duplicated or missing data points, but it's not a problem because we will build multiple trees using different samples.
## 5. 5. 2 Random Forest
---
We will first decide the number of samplings, which is also equal to the number of trees. If the number of sampling is too small, model may have high variance. On the other hand, if it is too large, it won’t change the predictions, but it will only increase the computational cost.

1. Classification
Each tree votes for a class, and the majority vote becomes the final prediction.

2. Regression
The final prediction is the average of all tree predictions.
## 5. 5. 3 Boosting
---
I will explain an intuition of Boosting.

1. All training data are initially assigned equal weights (importance).
2. Build the tree in the way we learned
3. Increase the weights of the data points with large errors, and decrease the weights of those with small errors.


# 5. 6 Comparison between Tree & NN
---
Let's compare when each algorithm is more useful.
1. Neural Networks
	- Works well on all types of data, including tabular and structured data
	- May be slower than a decision tree
	- Transfer learning is available
	- When building a system of multiple models working together, it might be easier to string together multiple neural networks.

2. Decision Tree
	- Works well on tabular data
	- Not recommended for unstructured data(image, audio, text, ...)
	- Fast
	- Small decision tree may be **human interpretable**.

In fact, single decision trees are mostly used because of their interpretability.