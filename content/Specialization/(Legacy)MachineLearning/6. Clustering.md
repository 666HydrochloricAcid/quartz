---
title: 6. Clustering
---
# 6. 0 Introduction
---
We have so far, learned the development flow of supervised learning. Now, we will turn our attention to **unsupervised learning**, where the model trains **without labels 'y'**. In particular, we will focus on **clustering** in this section.

![figure6-1][ML6-1.png]

As shown in the figure above, the data points are **clustered(grouped)** based on how close they are to one another(similarity), without any target values.

## 6. 1 K-Means Algorithm
---
K-means algorithm is a type of clustering technique. It groups data points based on euclidean distance, and we can choose the number of clusters. Let's take a look at its process.
## 6. 2 Process of K-Means
---
K-means algorithm divide datasets into K clusters as follow:

1. Choose $K$.
2. Randomly plot $K$ centroids.
	- Denote $K$ centroids as $\mu_1,\ \mu_2,\cdots,\ \mu_K$.
3. Assign the nearest centroid to each data points.
	- To measure the distance, we use L2 norm (euclidean distance).
	- Let $c^{(i)}$ be the index (from 1 to $K$) of centroid closest to the sample $x^{(i)}$.
4. For each cluster, compute the average of the points assigned to it and set this as the new centroid.
5. Repeat 3-4 until convergence.
	- when all centroids no longer change their position
	- when all cluster assignments no longer change

In step3, $^\forall i \in \{n\in\mathbb{N}|n\leq m\}$ we can formalize as follow:

$$
c^{(i)}=\arg\min_k||x^{(i)}-\mu_k||
$$

, where m is the number of the datas and, $k\in\mathbb{N}_K$.

In step 4, we can formalize as follow:

$$
\mu_k=\cfrac{1}{|C_k|}\sum_{x^{(i)}\in C_k}x^{(i)}
$$

,where $C_k$ is the set of data assigned $c_k$.

So, the cost function we want to minimize is the sum of all (squared) distance between $x^{(i)}$ and $\mu_{c^{(i)}}$ as follow:

$$
J(c^{(1)},\ c^{(2)},\cdots,\ c^{(m)},\ \mu_1,\ \mu_2,\cdots,\ \mu_k)=\sum^m_{i=1}||x^{(i)}-\mu_{c^{(i)}}||^2
$$
## 6. 3 Local Minima
---
This cost function also 
## 6. 4 Choosing K
---
