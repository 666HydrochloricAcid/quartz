---
aliases:
  - ML.4
tags:
---
# 3.0 Introduction
---
 이전 포스팅에서는, linearly separable dataset에서, model과 평과지표로 설정한 threshold를 decision boundary로 설정하여 분류를 진행했다. 이때, logistic function의 그래프 개형에 의해 decision boundary로부터 거리가 멀면 멀수록 분류에 대한 confidence가 높아진다.
 
 SVM에서는, 이러한 기하학적 관찰을 바탕으로 두 분류간의 거리가 최대한 멀게끔 decision boundary를 결정하여 binary classification을 수행한다.
# 3.1 Maximal Margin Classifier
---
 우선, SVM 및 이하 model에서는 target label set을 $\{0,\ 1\}$이 아닌 $\{-1,\ 1\}$로 두겠다. 이때, 0과 1은 각각 -1과 1의 순서대로 대응된다.
 
 #hyperplane 에 의해 분류되는 데이터들은 equation $\mathbf{Wx}+b=0$에 대하여, $\mathbf{Wx}+b>0$를 만족하는 데이터 $\mathbf{x}$에 대해서는, 1의 label을, 그렇지 않은 데이터는 -1의 label을 반환하여 분류된다. 이때, 











#Hyperplane #Non-Seperable
# 3.2 Support Vector Classifier
---
Allow slack variable violation adding $\epsilon$ 
# 3.3 Support Vector Machine (SVM)
---
#kernel
